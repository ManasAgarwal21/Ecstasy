import { initialState } from "../reducers/user.reducer";
import { productState } from "../reducers/user.reducer";

export const getUser = (state = initialState) => state;

export const getProducts = (state = productState) => state;
